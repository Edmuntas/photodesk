{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": ["-y", "firebase-tools@latest", "experimental:mcp"],
      "comment": "Official Firebase MCP — Firestore, Auth, Functions, Hosting"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "comment": "Official GitHub MCP — PRs, issues, code search, deployments"
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp"],
      "comment": "Microsoft Playwright MCP — E2E тесты, headless браузер, CI/CD"
    },
    "ux-expert": {
      "command": "npx",
      "args": ["-y", "@elsahafy/ux-mcp-server"],
      "comment": "UX Expert — 28 knowledge bases, 23 tools: WCAG, Nielsen heuristics, design systems"
    },
    "a11y": {
      "command": "npx",
      "args": ["-y", "a11y-mcp"],
      "comment": "Accessibility MCP — axe-core аудит WCAG прямо на страницах"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"],
      "comment": "Official MCP — структурированный анализ и многошаговое мышление"
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"],
      "comment": "Official MCP — постоянная память между сессиями (дизайн-решения, контекст)"
    }
  }
}
